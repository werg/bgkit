# @package _global_
# Full experiment preset: Phase 2 Step 5 (target LLM injection) with unfrozen BgKIT
defaults:
  - /config
  - /training/phase2
  - _self_

run_name: phase2_step5_unfrozen_bgkit
training:
  step5:
    freeze:
      bgkit_compressor: false
    lr:
      bgkit_compressor: 1.0e-5
