# @package _global_
# Full experiment preset: Phase 2 with unfrozen BgKIT
defaults:
  - /config
  - /training/phase2
  - _self_

run_name: phase2_unfrozen_bgkit
training:
  freeze:
    bgkit: false
  lr:
    bgkit: 1.0e-5
