# @package _global_
# Experiment preset: Phase 1 Step 4 pruned encoder reconstruction (LoRA decoder)
defaults:
  - /config
  - /training/phase1_step4
  - _self_

run_name: phase1_step4

# Load pruned encoder + decoder from step 2 distillation checkpoint
bgkit_checkpoint: auto
