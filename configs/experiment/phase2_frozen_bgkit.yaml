# @package _global_
# Full experiment preset: Phase 2 Step 5 (target LLM injection) with frozen BgKIT
defaults:
  - /config
  - /training/phase2_step5
  - _self_

run_name: phase2_step5_frozen_bgkit
training:
  freeze:
    bgkit_compressor: true
