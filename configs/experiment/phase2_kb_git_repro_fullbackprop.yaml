# @package _global_
# Experiment preset: Phase 2 git_commit_repro FULL-BACKPROP recursive-L1
# curriculum bootstrap. phase: phase2_kb -> KRKBTrainer with
# recursive_l1_tree.full_backprop: true (whole window subtree encoded LIVE;
# L0 cached/frozen; L1 + l1l1_bridge + tree trainable).
defaults:
  - /config
  - /training/phase2_kb_git_repro_fullbackprop
  - _self_

run_name: phase2_kb_git_repro_fullbackprop
# FRESH from summarization step51945 ("none" disables auto-resume). The
# 2026-07-03 opaque-BIP-39-ID + child-ID-injection redesign + data regen makes
# ALL prior git-repro checkpoints incompatible (they trained on positional IDs
# with no child-ID in the tree reps), so restart the task from the clean phase1
# base to learn navigation from the reps.
resume_checkpoint: "none"
