# @package _global_
# Experiment preset: Phase 2 git_commit_repro FULL-BACKPROP recursive-L1
# curriculum bootstrap. phase: phase2_kb -> KRKBTrainer with
# recursive_l1_tree.full_backprop: true (whole window subtree encoded LIVE;
# L0 cached/frozen; L1 + l1l1_bridge + tree trainable).
defaults:
  - /config
  - /training/phase2_kb_git_repro_fullbackprop
  - _self_

run_name: phase2_kb_git_repro_fullbackprop
# RESUME from the live run's latest checkpoint to load the enhanced ablation
# probe (nav/recon CE split) without losing training progress. The initial
# drill-down launch used "none" (fresh from summarization step51945); now we
# resume the in-progress run. Revert to "none" only for a from-scratch restart.
resume_checkpoint: /workspace/checkpoints/phase2_kb_step200_20260703_173150
