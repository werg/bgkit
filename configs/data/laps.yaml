mmap_dir: ${oc.env:DATA_DIR}/mmap/phase2/laps
max_document_len: 8192
max_question_len: 256
max_answer_len: 256
# LAPS: Long-term Accumulated Preferences - 11.2K prefs
# Two domains: recipe, movie
# Preferences accumulate across sessions, memory type: preference
