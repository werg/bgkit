# Auto-reproduction training configuration
phase: auto_repro
max_steps: 500
lr: 3.0e-6
warmup_steps: 50
# Only last transformer block trainable
freeze_except: ["last_block"]
eval_every: 100
save_every: 100
max_batch_tokens: 65536
max_eval_samples: 1000
